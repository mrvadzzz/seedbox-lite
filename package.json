{
  "name": "seedbox-lite",
  "version": "1.0.0",
  "description": "Локальный веб-интерфейс для просмотра разрешённого торрент-контента",
  "scripts": {
    "server": "npm --prefix server start",
    "client": "npm --prefix client run dev -- --host 0.0.0.0",
    "install-all": "npm --prefix server ci && npm --prefix client ci",
    "build": "npm --prefix client run build",
    "check": "node --check server/index.js && npm --prefix client run build"
  },
  "keywords": ["torrent", "streaming", "webtorrent"],
  "license": "SEE LICENSE IN LICENSE"
}
